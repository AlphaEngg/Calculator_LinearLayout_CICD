image: jlordiales/android-sdk:34-jdk17

pipelines:
  default:
    - step:
        name: Build APK
        caches:
          - gradle
        script:
          - echo "✅ Building Android App with public Android SDK image..."
          - chmod +x ./gradlew
          - ./gradlew clean assembleDebug
        artifacts:
          - app/build/outputs/**/*.apk
