image: ghcr.io/android-ci/android:android-34-jdk17

pipelines:
  default:
    - step:
        name: Build APK
        caches:
          - gradle
        script:
          - echo "✅ Building Android App with Android SDK + Java 17..."
          - chmod +x ./gradlew
          - ./gradlew clean assembleDebug
        artifacts:
          - app/build/outputs/**/*.apk
